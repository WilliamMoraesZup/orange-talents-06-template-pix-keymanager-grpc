datasources:
  default:
    url: jdbc:h2:mem:default
    username: sa
    password: ""
    driverClassName: org.h2.Driver

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true